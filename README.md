➜ ✗ python webscreenshot.py -h
```
usage: webscreenshot.py [-h] [-v VERBOSITY] [-u URL] [-f FILE] [-image FILE]
                        [-sleep SLEEP] [-size SIZE] [-proxy proxy]

optional arguments:
  -h, --help            show this help message and exit
  -v VERBOSITY, --verbosity VERBOSITY
                        increase output verbosity
  -u URL, --url URL     Target URL (e.g. 'http://www.example.com')
  -f FILE, --targets FILE
                        Target From FILENAME
  -image FILE, --image FILE
                        default disable image equal 2
  -sleep SLEEP, --sleep SLEEP
                        default sleep equal 0
  -size SIZE, --size SIZE
                        default size equal 1024x768
  -proxy proxy, --proxy proxy
                        Proxy (e.g. 'http://127.0.0.1:1087' or
                        'socks5://127.0.0.1:1080')
```
